/**
 * Coach · Exercise Library (Tab / Sidebar entry).
 *
 * The V2 Unified Exercise Library is now the DEFAULT experience for coaches.
 * Tapping "Library" in the mobile tab bar OR "Library" in the desktop sidebar
 * lands directly on the unified V2 screen — no V2 button, no migration banner.
 *
 * The legacy V1 exercise list is retained one-click away at /(coach)/library-legacy
 * as an admin fallback only, and is not surfaced anywhere in the default navigation.
 */
export { default } from "@/app/coach/exercise-content";
