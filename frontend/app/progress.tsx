/**
 * /progress — Iter 94t (Phase 3) — Goal-adaptive progress dashboard.
 */
import { ProgressDashboard } from "@/src/components/ProgressDashboard";

export default function ProgressScreen() {
  return <ProgressDashboard />;
}
